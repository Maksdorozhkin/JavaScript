// Шаблонные строки используем обратные кавычки
const hello = "Hello";
const world = "World";
const greeting = `${hello} ${world}`; // пример шаблонной строки
console.log(greeting);

// задание
const myName = "Maks";
const myCity = "Dedovsk";
console.log(
  `My name is ${myName}, i living in ${myCity.toLocaleLowerCase()} now`
);
