// forEach перебор элементов массива (выполняется функция для каждого элемента массива)

let arr = ["big tits", "milf", "big ass", "com in month"];
arr.forEach((item, index, array) => {
  console.log(`${item} находится на ${index} позиции в ${array}`);
});
